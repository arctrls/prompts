# FO-1280

## 📋 프로젝트 개요

**핵심 기능**: 국가별 계층적 행정구역 구조(1레벨→2레벨→3레벨)를 기반으로 한 드롭다운 메뉴용 행정구역 목록 제공

## 🎯 개발 목표

**TDD 방식으로 Prefecture 목록 조회 GraphQL API 구현**

## 📝 GraphQL Schema 설계

### 1. Type 정의

```graphql
# 행정구역 목록 요청 입력 타입 
input PrefectureRequest {
  level: Int!
  parentId: Int
  countryCode: Int
}

# 행정구역 데이터 타입 
type PrefectureResponse {
  id: Int!
  parentId: Int!
  position: String!
  level: Int!
  name: String!
  countryCode: Int!
  isActive: Boolean!
  isShippingEnabled: Boolean!
  zipCode: String
  address: String
  createdDate: String
}
```

### 2. Query 정의

```graphql
type Query {
  # 행정구역 목록 조회 (기존 /get_prefecture 대체)
  prefectures(request: PrefectureRequest!): [PrefectureResponse!]!
}
```
